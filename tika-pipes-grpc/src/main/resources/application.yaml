spring:
  application:
    name: tika-pipes
grpc:
  server:
    port: 50051
ignite:
  workDir: 'ignite/work'
  persistence:
    enabled: true
  memory:
    default:
      size: 1G
  sql:
    log: debug
forkparser:
  pool:
    core-size: 5
    max-size: 10
    queue-capacity: 100
